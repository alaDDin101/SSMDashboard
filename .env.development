# Leave empty to use same-origin /api and /uploads (Vite proxies both to the API — see vite.config.ts).
# Or set the full API origin, e.g. VITE_API_BASE=http://localhost:5271
VITE_API_BASE=https://syriasonsmovement-production.up.railway.app
